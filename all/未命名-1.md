```mermaid
graph LR
    subgraph Pickup
        A[Courier Driver] --> B{Search Pickup Request by Customer Name}
        B -- Found --> C{Display Pickup Request}
        B -- Not Found --> D{Display "None"}
    end
    subgraph Package Entry
        C --> E{Enter Package Information}
        E --> F{Create Pickup Request}
        F --> G{Display Pickup Request}
    end
    subgraph Payment Options
        G --> H{Customer chooses Cash}
        H --> I{Issue Receipt}
        G --> J{Customer chooses Other Payment}
        J --> K{Issue Payment Reference Number}
```